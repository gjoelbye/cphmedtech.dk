{
  "name": "cphmedtech.dk",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node -e \"const fs=require('fs'),f='dist/server/wrangler.json',c=JSON.parse(fs.readFileSync(f,'utf8'));delete c.pages_build_output_dir;fs.writeFileSync(f,JSON.stringify(c));\"",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.1.3",
    "@astrojs/mdx": "^5.0.2",
    "astro": "^6.0.8",
    "citation-js": "^0.7.22"
  },
  "devDependencies": {
    "sass": "^1.98.0"
  }
}
